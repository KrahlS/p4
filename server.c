#include mkfs.c // might need to call this in the Makefile instead of include it here, so it builds the fs before the program is run
#include mfs.h

int main(int argc, char *argv[]) {
    // portnum = argv[1]
    // mmap filesystem image (argv[2])
    while(1) {
        // scan input and respond
    }
}